<!DOCTYPE html>
<head>
<title>Todo App</title>
<link rel="stylesheet" href="/static/stylesheets/style.css" type="text/css">
<link href="https://fonts.googleapis.com/css?family=Baloo+Bhai|Candal|Chewy&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
</head>
<body>
<div class="todo-container">
<h2>To-Do List</h2>
<div class="todo">
    <!-- Use POST method to support new upload-->
<form action="" method="POST" class="todo-header">
<input type="text" name="content">
<button type="submit"><span class="fas fa-plus"></span></button>
</form>
<ul class="todo-list">
<% todoTasks.forEach(details => { %>
<% if (details._id == idTask) { %>   <!-- Check if the task id is the same as the id of details -->
<li class="todo-list-item">
    <!-- for each Todo item, an edit field is displayed -->
<form action="/edit/<%= details._id %>" method="POST">
<input type="text" value="<%= details.content %>" name="content">
<button type="submit">Confirm</button>
<a href="/" class="cancel">Cancel</a>
</form>
</li>
<% } else { %>
<li class="todo-list-item">
<div class="todo-list-item-name"><%= details.content %></div>   <!-- Display details -->
<a href="/edit/<%= details._id %>" class="edit"> <span class="fas fa-edit"></span>   <!-- Editing link here -->
</a>
<a href="/remove/<%= details._id %>" class="remove"> <span class="fas fa-times"></span>   <!-- Deleting link here -->
</a>
</li>
<% } %>
<% }) %>
</ul>
</div>
</div>
</body>